# ── Web framework ──────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# ── Database ───────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0          # async PostgreSQL driver
psycopg2-binary>=2.9,<3.0   # sync PostgreSQL driver (for scripts, etc.)
alembic>=1.13,<2.0
redis[hiredis]>=5.0,<6.0

# ── Auth ───────────────────────────────────────────────────
pyjwt>=2.8,<3.0
passlib[bcrypt]>=1.7,<2.0
python-multipart>=0.0.9
email-validator>=2.1,<3.0       # Pydantic EmailStr validation

# ── Config ─────────────────────────────────────────────────
pydantic-settings>=2.5,<3.0
pydantic>=2.5,<3.0

# ── Crypto (RS256 key generation, JWT signing) ─────────────
cryptography>=42.0,<44.0

# ── CORS ───────────────────────────────────────────────────
# (included in FastAPI — starlette)

# ── SSE ────────────────────────────────────────────────────
sse-starlette>=2.0,<3.0        # Server-Sent Events support

# ── MQTT ───────────────────────────────────────────────────
gmqtt>=0.6,<1.0                # Async MQTT 5.0 client

# ── Dev / Debug / Testing ──────────────────────────────────
# python-dotenv>=1.0
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.24
pytest-mock>=3.12
aiosqlite>=0.20,<1.0        # In-memory SQLite for unit tests
